# NPM Configuration for hospital_appointment

# Use legacy peer dependency resolution to handle ESLint 9 migration
# Many React Native plugins haven't updated for ESLint 9 yet
legacy-peer-deps=true

# Strict engine checking
engine-strict=true

# Save exact versions
save-exact=true

# Audit level
audit-level=moderate
