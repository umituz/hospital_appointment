/**
 * Core Components - Barrel Export
 *
 * Centralized exports for all core app components.
 * ScreenLayout now comes directly from design system (no wrapper needed).
 */

// Re-export ScreenLayout from design system (no wrapper needed - auto theme-aware)
export { ScreenLayout } from '@umituz/react-native-design-system';

export { AppStatusBar } from './AppStatusBar';
